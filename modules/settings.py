from utils.settings_manager import settings_manager

def load_settings_to_layout():
    settings = settings_manager.get_settings()
    # Logic to populate UI elements with settings
    return settings
